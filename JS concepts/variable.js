const accountId = 13234;
let accountEmail = "akshay@12.com"
var accountPassword = "12345"
accountCity = "Bihar" // not good practice 

console.table([
    accountId, accountEmail, accountPassword, accountCity
]);

/* console.log([
    accountId, accountEmail, accountPassword, accountCity
]); */

// mostly we prefer to use let insted of var
// Use const by default.
// Use let when the value will change.

